import { c as cn, B as Button, g as getAnnotationData, M as MessageAnnotationType } from './actions-12s-M34siTXk.js';
export { e as extractArtifactsFromAllMessages, a as extractArtifactsFromMessage, d as getInlineAnnotations, i as isEqualArtifact, b as isMessageAnnotation, p as parseInlineAnnotation, t as toInlineAnnotation } from './actions-12s-M34siTXk.js';
import { _ as _$1 } from '@swc/helpers/_/_extends';
import { jsx, jsxs, Fragment } from 'react/jsx-runtime';
import * as React from 'react';
import { createContext, useState, useContext, useRef, memo, useMemo, useEffect } from 'react';
import { _ as _$2 } from '@swc/helpers/_/_async_to_generator';
import { Square, Send, Bot, Check, Copy, RefreshCw, Loader2, PauseCircle } from 'lucide-react';
import { _ } from '@swc/helpers/_/_object_without_properties_loose';
import { FileUploader, ChatEvents, ChatAgentEvents, ChatImage, ChatFiles, ChatSources, SuggestedQuestions, Markdown } from '../widgets/index.js';
import { a as useChatUI, C as ChatProvider, b as ChatCanvasProvider } from './context-12s-DUrdLaCB.js';
export { u as useChatCanvas } from './context-12s-DUrdLaCB.js';
import { u as useCopyToClipboard } from './use-copy-to-clipboard-12s-B94o_fsN.js';
import { C as ChatCanvas } from './index-12s-CwvXCJ6Q.js';
export { u as useFile } from './use-file-12s-BO6ECMlO.js';

const Textarea = /*#__PURE__*/ React.forwardRef((_param, ref)=>{
    var { className } = _param, props = _(_param, [
        "className"
    ]);
    return /*#__PURE__*/ jsx("textarea", _$1({
        className: cn('border-input bg-background ring-offset-background placeholder:text-muted-foreground focus-visible:ring-ring focus-visible:outline-hidden flex min-h-[80px] w-full rounded-md border px-3 py-2 text-sm focus-visible:ring-2 focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50', className),
        ref: ref
    }, props));
});
Textarea.displayName = 'Textarea';

const chatInputContext = /*#__PURE__*/ createContext(null);
const ChatInputProvider = chatInputContext.Provider;
const useChatInput = ()=>{
    const context = useContext(chatInputContext);
    if (!context) {
        throw new Error('useChatInput must be used within a ChatInputProvider');
    }
    return context;
};
function ChatInput(props) {
    const { input, setInput, append, isLoading, requestData } = useChatUI();
    const isDisabled = isLoading || !input.trim();
    const [isComposing, setIsComposing] = useState(false);
    const submit = ()=>_$2(function*() {
            const newMessage = {
                role: 'user',
                content: input,
                annotations: props.annotations
            };
            setInput('') // Clear the input
            ;
            props.resetUploadedFiles == null ? void 0 : props.resetUploadedFiles.call(props) // Reset the uploaded files
            ;
            yield append(newMessage, {
                data: requestData
            });
        })();
    const handleSubmit = (e)=>_$2(function*() {
            e.preventDefault();
            yield submit();
        })();
    const handleKeyDown = (e)=>_$2(function*() {
            if (isDisabled) return;
            if (e.key === 'Enter' && !e.shiftKey && !isComposing) {
                e.preventDefault();
                yield submit();
            }
        })();
    var _props_children;
    const children = (_props_children = props.children) != null ? _props_children : /*#__PURE__*/ jsx(ChatInputForm, {});
    return /*#__PURE__*/ jsx(ChatInputProvider, {
        value: {
            isDisabled,
            handleKeyDown,
            handleSubmit,
            isComposing,
            setIsComposing
        },
        children: /*#__PURE__*/ jsx("div", {
            className: cn('bg-background flex shrink-0 flex-col gap-4 p-4 pt-0', props.className),
            children: children
        })
    });
}
function ChatInputForm(props) {
    const { handleSubmit } = useChatInput();
    var _props_children;
    const children = (_props_children = props.children) != null ? _props_children : /*#__PURE__*/ jsxs(Fragment, {
        children: [
            /*#__PURE__*/ jsx(ChatInputField, {}),
            /*#__PURE__*/ jsx(ChatInputSubmit, {})
        ]
    });
    return /*#__PURE__*/ jsx("form", {
        onSubmit: handleSubmit,
        className: cn('relative flex gap-2', props.className),
        children: children
    });
}
function ChatInputField(props) {
    const { input, setInput } = useChatUI();
    const { handleKeyDown, setIsComposing } = useChatInput();
    const textareaRef = useRef(null);
    // auto resize the textarea based on the content
    const handleInputChange = (e)=>{
        setInput(e.target.value);
        if (textareaRef.current) {
            textareaRef.current.style.height = 'auto';
            let newHeight = Math.max(textareaRef.current.scrollHeight, 100);
            if (textareaRef.current.scrollHeight > 80) {
                newHeight += 40 // offset for the textarea padding
                ;
            }
            textareaRef.current.style.height = `${newHeight}px`;
        }
    };
    var _props_placeholder;
    return /*#__PURE__*/ jsx(Textarea, {
        ref: textareaRef,
        name: "input",
        placeholder: (_props_placeholder = props.placeholder) != null ? _props_placeholder : 'Type a message...',
        className: cn('bg-secondary h-[100px] max-h-[400px] min-h-0 flex-1 resize-none overflow-y-auto rounded-2xl p-4', props.className),
        value: input,
        onChange: handleInputChange,
        onKeyDown: handleKeyDown,
        onCompositionStart: ()=>setIsComposing(true),
        onCompositionEnd: ()=>setIsComposing(false),
        spellCheck: false
    });
}
function ChatInputUpload(props) {
    const { requestData, setRequestData, isLoading } = useChatUI();
    const onFileUpload = (file)=>_$2(function*() {
            if (props.onUpload) {
                yield props.onUpload(file);
            } else {
                setRequestData(_$1({}, requestData || {}, {
                    file
                }));
            }
        })();
    var _props_multiple;
    return /*#__PURE__*/ jsx(FileUploader, {
        onFileUpload: onFileUpload,
        config: {
            disabled: isLoading,
            multiple: (_props_multiple = props.multiple) != null ? _props_multiple : true,
            allowedExtensions: props.allowedExtensions
        },
        className: cn('hover:bg-primary absolute bottom-2 left-2 rounded-full', props.className)
    });
}
function ChatInputSubmit(props) {
    const { stop, isLoading } = useChatUI();
    const { isDisabled } = useChatInput();
    if (stop && isLoading) {
        return /*#__PURE__*/ jsx(Button, {
            size: "icon",
            onClick: stop,
            className: "absolute bottom-2 right-2 rounded-full",
            children: /*#__PURE__*/ jsx(Square, {
                className: "size-3",
                fill: "white",
                stroke: "white"
            })
        });
    }
    var _props_disabled, _props_children;
    return /*#__PURE__*/ jsx(Button, {
        type: "submit",
        size: "icon",
        disabled: (_props_disabled = props.disabled) != null ? _props_disabled : isDisabled,
        className: cn('absolute bottom-2 right-2 rounded-full', props.className),
        children: (_props_children = props.children) != null ? _props_children : /*#__PURE__*/ jsx(Send, {
            className: "size-4"
        })
    });
}
ChatInput.Form = ChatInputForm;
ChatInput.Field = ChatInputField;
ChatInput.Upload = ChatInputUpload;
ChatInput.Submit = ChatInputSubmit;

const chatMessageContext = createContext(null);
const ChatMessageProvider = chatMessageContext.Provider;
const useChatMessage = ()=>{
    const context = useContext(chatMessageContext);
    if (!context) throw new Error('useChatMessage must be used within a ChatMessageProvider');
    return context;
};

function EventAnnotations() {
    const { message, isLast, isLoading } = useChatMessage();
    var _ref;
    const showLoading = (_ref = isLast && isLoading) != null ? _ref : false;
    const eventData = getAnnotationData(message, MessageAnnotationType.EVENTS);
    if (eventData.length === 0) return null;
    return /*#__PURE__*/ jsx(ChatEvents, {
        data: eventData,
        showLoading: showLoading
    });
}
function AgentEventAnnotations() {
    const { message, isLast } = useChatMessage();
    const agentEventData = getAnnotationData(message, MessageAnnotationType.AGENT_EVENTS);
    if (agentEventData.length === 0) return null;
    return /*#__PURE__*/ jsx(ChatAgentEvents, {
        data: agentEventData,
        isFinished: Boolean(message.content),
        isLast: isLast
    });
}
function ImageAnnotations() {
    const { message } = useChatMessage();
    const imageData = getAnnotationData(message, 'image');
    if (imageData.length === 0) return null;
    return /*#__PURE__*/ jsx(ChatImage, {
        data: imageData[0]
    });
}
function DocumentFileAnnotations() {
    const { message } = useChatMessage();
    const contentFileData = getAnnotationData(message, MessageAnnotationType.DOCUMENT_FILE);
    if (contentFileData.length === 0) return null;
    const alignmentClass = message.role === 'user' ? 'ml-auto' : 'mr-auto';
    return /*#__PURE__*/ jsx(ChatFiles, {
        data: contentFileData[0],
        className: alignmentClass
    });
}
function preprocessSourceNodes(nodes) {
    // Filter source nodes has lower score
    const processedNodes = nodes.map((node)=>{
        // remove trailing slash for node url if exists
        if (node.url) {
            node.url = node.url.replace(/\/$/, '');
        }
        return node;
    });
    return processedNodes;
}
function getSourceNodes(message) {
    const data = getAnnotationData(message, MessageAnnotationType.SOURCES);
    return data.map((item)=>_$1({}, item, {
            nodes: item.nodes ? preprocessSourceNodes(item.nodes) : []
        })).flatMap((item)=>item.nodes);
}
function SourceAnnotations() {
    const { message } = useChatMessage();
    const nodes = getSourceNodes(message);
    if (nodes.length === 0) return null;
    return /*#__PURE__*/ jsx(ChatSources, {
        data: {
            nodes
        }
    });
}
function SuggestedQuestionsAnnotations() {
    const { append, requestData } = useChatUI();
    const { message, isLast } = useChatMessage();
    if (!isLast || !append) return null;
    const suggestedQuestionsData = getAnnotationData(message, MessageAnnotationType.SUGGESTED_QUESTIONS);
    if (suggestedQuestionsData.length === 0) return null;
    return /*#__PURE__*/ jsx(SuggestedQuestions, {
        questions: suggestedQuestionsData[0],
        append: append,
        requestData: requestData
    });
}

const defaultAnnotationRenderers = {
    artifact: ChatCanvas.Artifact
};

var ContentPosition = /*#__PURE__*/ function(ContentPosition) {
    ContentPosition[ContentPosition["TOP"] = -9999] = "TOP";
    ContentPosition[ContentPosition["CHAT_EVENTS"] = 0] = "CHAT_EVENTS";
    ContentPosition[ContentPosition["AFTER_EVENTS"] = 1] = "AFTER_EVENTS";
    ContentPosition[ContentPosition["CHAT_AGENT_EVENTS"] = 2] = "CHAT_AGENT_EVENTS";
    ContentPosition[ContentPosition["AFTER_AGENT_EVENTS"] = 3] = "AFTER_AGENT_EVENTS";
    ContentPosition[ContentPosition["CHAT_IMAGE"] = 4] = "CHAT_IMAGE";
    ContentPosition[ContentPosition["AFTER_IMAGE"] = 5] = "AFTER_IMAGE";
    ContentPosition[ContentPosition["BEFORE_MARKDOWN"] = 6] = "BEFORE_MARKDOWN";
    ContentPosition[ContentPosition["MARKDOWN"] = 7] = "MARKDOWN";
    ContentPosition[ContentPosition["AFTER_MARKDOWN"] = 8] = "AFTER_MARKDOWN";
    ContentPosition[ContentPosition["CHAT_DOCUMENT_FILES"] = 9] = "CHAT_DOCUMENT_FILES";
    ContentPosition[ContentPosition["AFTER_DOCUMENT_FILES"] = 10] = "AFTER_DOCUMENT_FILES";
    ContentPosition[ContentPosition["CHAT_SOURCES"] = 11] = "CHAT_SOURCES";
    ContentPosition[ContentPosition["AFTER_SOURCES"] = 12] = "AFTER_SOURCES";
    ContentPosition[ContentPosition["SUGGESTED_QUESTIONS"] = 13] = "SUGGESTED_QUESTIONS";
    ContentPosition[ContentPosition["AFTER_SUGGESTED_QUESTIONS"] = 14] = "AFTER_SUGGESTED_QUESTIONS";
    ContentPosition[ContentPosition["BOTTOM"] = 9999] = "BOTTOM";
    return ContentPosition;
}({});
function ChatMessage(props) {
    var _props_children;
    const children = (_props_children = props.children) != null ? _props_children : /*#__PURE__*/ jsxs(Fragment, {
        children: [
            /*#__PURE__*/ jsx(ChatMessageAvatar, {}),
            /*#__PURE__*/ jsx(ChatMessageContent, {
                isLoading: props.isLoading,
                append: props.append
            }),
            /*#__PURE__*/ jsx(ChatMessageActions, {})
        ]
    });
    return /*#__PURE__*/ jsx(ChatMessageProvider, {
        value: {
            message: props.message,
            isLast: props.isLast,
            isLoading: props.isLoading,
            append: props.append
        },
        children: /*#__PURE__*/ jsx("div", {
            className: cn('group flex gap-4 p-3', props.className),
            children: children
        })
    });
}
function ChatMessageAvatar(props) {
    const { message } = useChatMessage();
    if (message.role !== 'assistant') return null;
    var _props_children;
    const children = (_props_children = props.children) != null ? _props_children : /*#__PURE__*/ jsx(Bot, {
        className: "h-4 w-4"
    });
    return /*#__PURE__*/ jsx("div", {
        className: cn('bg-background flex h-8 w-8 shrink-0 select-none items-center justify-center rounded-full border', props.className),
        children: children
    });
}
function ChatMessageContent(props) {
    var _props_children;
    const children = (_props_children = props.children) != null ? _props_children : /*#__PURE__*/ jsxs(Fragment, {
        children: [
            /*#__PURE__*/ jsx(EventAnnotations, {}),
            /*#__PURE__*/ jsx(AgentEventAnnotations, {}),
            /*#__PURE__*/ jsx(ImageAnnotations, {}),
            /*#__PURE__*/ jsx(ChatMarkdown, {}),
            /*#__PURE__*/ jsx(DocumentFileAnnotations, {}),
            /*#__PURE__*/ jsx(SourceAnnotations, {}),
            /*#__PURE__*/ jsx(SuggestedQuestionsAnnotations, {})
        ]
    });
    return /*#__PURE__*/ jsx("div", {
        className: cn('flex min-w-0 flex-1 flex-col gap-4', props.className),
        children: children
    });
}
function ChatMarkdown(props) {
    const { message } = useChatMessage();
    const nodes = useMemo(()=>getSourceNodes(message), [
        message
    ]);
    var _props_annotationRenderers;
    return /*#__PURE__*/ jsx(Markdown, {
        content: message.content,
        sources: {
            nodes
        },
        citationComponent: props.citationComponent,
        languageRenderers: props.languageRenderers,
        annotationRenderers: (_props_annotationRenderers = props.annotationRenderers) != null ? _props_annotationRenderers : defaultAnnotationRenderers,
        className: cn({
            'bg-primary text-primary-foreground ml-auto w-fit max-w-[80%] rounded-xl px-3 py-2': message.role === 'user'
        }, props.className)
    });
}
function ChatMessageActions(props) {
    const { reload, requestData, isLoading } = useChatUI();
    const { isCopied, copyToClipboard } = useCopyToClipboard({
        timeout: 2000
    });
    const { message, isLast } = useChatMessage();
    if (message.role !== 'assistant') return null;
    const isLastMessageFromAssistant = message.role === 'assistant' && isLast;
    const showReload = reload && !isLoading && isLastMessageFromAssistant;
    var _props_children;
    const children = (_props_children = props.children) != null ? _props_children : /*#__PURE__*/ jsxs(Fragment, {
        children: [
            /*#__PURE__*/ jsx(Button, {
                title: "Copy",
                onClick: ()=>copyToClipboard(message.content),
                size: "icon",
                variant: "outline",
                className: "h-8 w-8",
                children: isCopied ? /*#__PURE__*/ jsx(Check, {
                    className: "h-4 w-4"
                }) : /*#__PURE__*/ jsx(Copy, {
                    className: "h-4 w-4"
                })
            }),
            showReload && /*#__PURE__*/ jsx(Button, {
                title: "Regenerate",
                variant: "outline",
                size: "icon",
                onClick: ()=>reload == null ? void 0 : reload({
                        data: requestData
                    }),
                className: "h-8 w-8",
                children: /*#__PURE__*/ jsx(RefreshCw, {
                    className: "h-4 w-4"
                })
            })
        ]
    });
    return /*#__PURE__*/ jsx("div", {
        className: cn('flex shrink-0 flex-col gap-2', props.className),
        children: children
    });
}
const PrimiviteChatMessage = /*#__PURE__*/ memo(ChatMessage, (prevProps, nextProps)=>{
    return !nextProps.isLast && prevProps.isLast === nextProps.isLast && prevProps.message === nextProps.message;
});
PrimiviteChatMessage.Content = ChatMessageContent;
PrimiviteChatMessage.Content.Event = EventAnnotations;
PrimiviteChatMessage.Content.AgentEvent = AgentEventAnnotations;
PrimiviteChatMessage.Content.Image = ImageAnnotations;
PrimiviteChatMessage.Content.Markdown = ChatMarkdown;
PrimiviteChatMessage.Content.DocumentFile = DocumentFileAnnotations;
PrimiviteChatMessage.Content.Source = SourceAnnotations;
PrimiviteChatMessage.Content.SuggestedQuestions = SuggestedQuestionsAnnotations;
PrimiviteChatMessage.Avatar = ChatMessageAvatar;
PrimiviteChatMessage.Actions = ChatMessageActions;

const chatMessagesContext = /*#__PURE__*/ createContext(null);
const ChatMessagesProvider = chatMessagesContext.Provider;
const useChatMessages = ()=>{
    const context = useContext(chatMessagesContext);
    if (!context) {
        throw new Error('useChatMessages must be used within a ChatMessagesProvider');
    }
    return context;
};
function ChatMessages(props) {
    const { messages, reload, stop, isLoading } = useChatUI();
    const messageLength = messages.length;
    const lastMessage = messages[messageLength - 1];
    const isLastMessageFromAssistant = messageLength > 0 && (lastMessage == null ? void 0 : lastMessage.role) !== 'user';
    const showReload = reload && !isLoading && isLastMessageFromAssistant;
    const showStop = stop && isLoading;
    // `isPending` indicate
    // that stream response is not yet received from the server,
    // so we show a loading indicator to give a better UX.
    const isPending = isLoading && !isLastMessageFromAssistant;
    var _props_children;
    const children = (_props_children = props.children) != null ? _props_children : /*#__PURE__*/ jsx(ChatMessagesList, {});
    return /*#__PURE__*/ jsx(ChatMessagesProvider, {
        value: {
            isPending,
            showReload,
            showStop,
            lastMessage,
            messageLength
        },
        children: /*#__PURE__*/ jsx("div", {
            className: cn('bg-background relative flex min-h-0 flex-1 flex-col space-y-6 p-4 pb-0', props.className),
            children: children
        })
    });
}
function ChatMessagesList(props) {
    const scrollableChatContainerRef = useRef(null);
    const { messages, isLoading, append } = useChatUI();
    const { lastMessage, messageLength } = useChatMessages();
    const scrollToBottom = ()=>{
        if (scrollableChatContainerRef.current) {
            scrollableChatContainerRef.current.scrollTop = scrollableChatContainerRef.current.scrollHeight;
        }
    };
    useEffect(()=>{
        scrollToBottom();
    }, [
        messageLength,
        lastMessage
    ]);
    var _props_children;
    const children = (_props_children = props.children) != null ? _props_children : /*#__PURE__*/ jsxs(Fragment, {
        children: [
            messages.map((message, index)=>{
                return /*#__PURE__*/ jsx(PrimiviteChatMessage, {
                    message: message,
                    isLast: index === messageLength - 1,
                    isLoading: isLoading,
                    append: append
                }, index);
            }),
            /*#__PURE__*/ jsx(ChatMessagesEmpty, {}),
            /*#__PURE__*/ jsx(ChatMessagesLoading, {})
        ]
    });
    return /*#__PURE__*/ jsx("div", {
        className: cn('flex min-h-0 flex-1 flex-col gap-5 overflow-auto', props.className),
        ref: scrollableChatContainerRef,
        children: children
    });
}
function ChatMessagesEmpty(props) {
    const { messages } = useChatUI();
    if (messages.length > 0) return null;
    if (props.children) {
        return /*#__PURE__*/ jsx("div", {
            className: cn('flex h-full flex-col justify-center pt-4', props.className),
            children: props.children
        });
    }
    var _props_heading, _props_subheading;
    return /*#__PURE__*/ jsxs("div", {
        className: cn('flex h-full flex-col justify-center pt-4', props.className),
        children: [
            /*#__PURE__*/ jsx("p", {
                className: "mb-2 animate-[slide-up_0.5s_ease-out] text-3xl font-bold opacity-0 [animation-delay:100ms] [animation-fill-mode:forwards]",
                children: (_props_heading = props.heading) != null ? _props_heading : 'Hello there!'
            }),
            /*#__PURE__*/ jsx("p", {
                className: "text-muted-foreground animate-[slide-up_0.5s_ease-out] text-xl opacity-0 [animation-delay:300ms] [animation-fill-mode:forwards]",
                children: (_props_subheading = props.subheading) != null ? _props_subheading : "I'm here to help you with your questions."
            }),
            /*#__PURE__*/ jsx("style", {
                children: `
        @keyframes slide-up {
          from {
            opacity: 0;
            transform: translateY(20px);
          }
          to {
            opacity: 1;
            transform: translateY(0);
          }
        }
        .animate-slide-up {
          animation: slide-up 0.5s ease-out;
        }
      `
            })
        ]
    });
}
function ChatMessagesLoading(props) {
    const { isPending } = useChatMessages();
    if (!isPending) return null;
    var _props_children;
    const children = (_props_children = props.children) != null ? _props_children : /*#__PURE__*/ jsx(Loader2, {
        className: "h-4 w-4 animate-spin"
    });
    return /*#__PURE__*/ jsx("div", {
        className: cn('flex items-center justify-center pt-4', props.className),
        children: children
    });
}
function ChatActions(props) {
    const { reload, stop, requestData } = useChatUI();
    const { showReload, showStop } = useChatMessages();
    if (!showStop && !showReload) return null;
    var _props_children;
    const children = (_props_children = props.children) != null ? _props_children : /*#__PURE__*/ jsxs(Fragment, {
        children: [
            showStop && /*#__PURE__*/ jsxs(Button, {
                variant: "outline",
                size: "sm",
                onClick: stop,
                children: [
                    /*#__PURE__*/ jsx(PauseCircle, {
                        className: "mr-2 h-4 w-4"
                    }),
                    "Stop generating"
                ]
            }),
            showReload && /*#__PURE__*/ jsxs(Button, {
                variant: "outline",
                size: "sm",
                onClick: ()=>reload == null ? void 0 : reload({
                        data: requestData
                    }),
                children: [
                    /*#__PURE__*/ jsx(RefreshCw, {
                        className: "mr-2 h-4 w-4"
                    }),
                    "Regenerate"
                ]
            })
        ]
    });
    return /*#__PURE__*/ jsx("div", {
        className: cn('flex justify-end gap-4', props.className),
        children: children
    });
}
ChatMessages.List = ChatMessagesList;
ChatMessages.Loading = ChatMessagesLoading;
ChatMessages.Empty = ChatMessagesEmpty;
ChatMessages.Actions = ChatActions;

function ChatSection(props) {
    const { handler, className } = props;
    const [requestData, setRequestData] = useState();
    var _props_children;
    const children = (_props_children = props.children) != null ? _props_children : /*#__PURE__*/ jsxs(Fragment, {
        children: [
            /*#__PURE__*/ jsx(ChatMessages, {}),
            /*#__PURE__*/ jsx(ChatInput, {})
        ]
    });
    return /*#__PURE__*/ jsx(ChatProvider, {
        value: _$1({}, handler, {
            requestData,
            setRequestData
        }),
        children: /*#__PURE__*/ jsx("div", {
            className: cn('flex h-full w-full flex-col gap-4 p-5', className),
            children: /*#__PURE__*/ jsx(ChatCanvasProvider, {
                children: children
            })
        })
    });
}

export { ChatCanvas, ChatInput, PrimiviteChatMessage as ChatMessage, ChatMessages, ChatSection, ContentPosition, MessageAnnotationType, defaultAnnotationRenderers, getAnnotationData, useChatInput, useChatMessage, useChatMessages, useChatUI };
